/**
 * DON'T ERASE THIS FILE
 * Functions on this File need to be moved to globals as we move forward
 * If any developer finds a function here which is already placed on globals please remove it from here and make the updates needed
 */

export enum DateFormatting {
  VERBOSE = 'dddd, MMMM Do YYYY, h:mm:ss a',
}

export const TimeRangeRegex = /^(\d+|\d+\,{1}\d+)$/;
